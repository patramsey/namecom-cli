# Build artifacts
/namecom
/namecom-cli
/dist/

# Local config / credentials — never commit
*.local.yaml
.namecom.yaml

# Go workspace (local multi-module development)
go.work
go.work.sum

# Agent working artifacts — local only, never published. Planning docs,
# task briefs, and review diffs are a record of how the work was produced,
# not something a contributor reading this repo needs.
.claude/
CLAUDE.md
.superpowers/
docs/plans/

# Editor / OS noise
.DS_Store
*.swp
